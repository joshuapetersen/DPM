# DPM UNIVERSAL SUBSTRATE INTERFACE (USI)

### HOW TO HOOK YOUR MODEL INTO THE DPM TOWER
The **Dynamic Parameter Model (DPM)** does not replace your model—it **amplifies** it. This guide shows you how to connect *any* intelligence source to the DPM logic stack.

---

### ARCHITECTURAL FLOW:
1.  **QUERY INPUT**: A complex request enters the **Intelligence Amplifier.**
2.  **DECOMPOSITION**: The DPM breaks the request into atomic tasks.
3.  **SUBSTRATE CALL**: The DPM sends these atomic tasks to your **Substrate** (Ollama, GPT, Claude, or local code).
4.  **RESONANCE GATING**: Each result is filtered by the **1.0927 GHz Frequency** and the **Billion Barrier.**
5.  **SYNTHESIS**: Results are combined into a high-density reasoning output (120B+ simulation).

---

### CONNECTING YOUR MODEL:
To connect a new model, update your `TinyRuntime.py` gateway with one of the following:

#### LOCAL MESH GATEWAY (GGUF/Ollama):
Point the `mesh_url` to your local inference endpoint (e.g., `http://localhost:11434/api/generate`).

#### EXTERNAL API GATEWAY (GPT/Claude):
Swap the `mesh_url` for your cloud API endpoint. The DPM remains the **Sovereign Hypervisor** regardless of where the data comes from.

#### DIRECT BINARY HOOK (C++/Genlex):
The DPM can talk directly to native logic via the **TheoryLab** (Sovereign_Math.py). This is the fastest, 100% offline configuration.

---
**[ THE DPM IS THE UNIVERSAL REASONING HYPERVISOR ]**
